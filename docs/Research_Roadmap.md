# 🚀 HTR-VT Project Roadmap: From Reproduction to SOTA (2025-2026)

## Phase 1: Baseline Reproduction (复现阶段) [Current Stage]

**目标**: 跑通代码，在 IAM/LAM 数据集上复现论文 Table 2 & 4 的指标，建立可信的 Baseline。

- [ ] **E1.1 环境构建**: 配置 PyTorch, timm, 准备数据集 (IAM/LAM/READ2016)。
- [ ] **E1.2 训练复现**:
  - 运行 `run/iam.sh`，确保 Loss 正常下降。
  - 验证 `Span Mask` 和 `SAM` 的有效性（对应论文 Table 5 Ablation Study）。
- [ ] **E1.3 指标对齐**:
  - [cite_start]Target (IAM): CER ~3.3%, WER ~10.8% [cite: 796]。
  - [cite_start]Target (LAM): CER ~2.6%, WER ~6.9% [cite: 796]。

## Phase 2: Diagnosis & Analysis (诊断与瓶颈分析)

**目标**: 找出 HTR-VT 的弱点 (Failure Cases)，为改进找方向。

- [ ] **D2.1 错误分析**: 导出 Test Set 的预测结果，按 CER 排序，挑出 Top-50 坏例 (Bad Cases)。
- [ ] **D2.2 归因分析**:
  - 是视觉特征提取不足？(ResNet18 够吗？)
  - 是序列建模能力不足？(CTC 无法利用语言上下文，如 "apple" 拼成 "apqle")。
  - 是对形变/噪声敏感？
- [ ] **D2.3 Attention 可视化**: 复现论文 Fig 2，检查 Mask 机制是否真的让模型关注了更宽的上下文。

## Phase 3: Innovation Candidates (改进路线备选)

**目标**: 确定 2026 论文的 Story 和核心创新点 (选择其一或组合)。

### 💡 Direction A: 增强序列依赖 (Language-Awareness)

- [cite_start]**痛点**: HTR-VT 仅使用 CTC Loss [cite: 581, 632]，CTC 假设字符间独立，缺乏语言模型能力。
- **改进**: 引入 **Global Pointer** 或 **Lightweight Decoder** (Non-Autoregressive)，在不显著增加推理时间的前提下，引入字符级依赖。
- **预期**: 显著降低 WER (Word Error Rate)。

### 💡 Direction B: 视觉增强 (Visual Robustness)

- [cite_start]**痛点**: 简单的 ResNet-18 [cite: 583] 可能无法处理极度潦草或形变严重的字体。
- **改进**: 引入 **TPS (Thin-Plate Spline)** 变换网络作为前处理，或将 ResNet 替换为带有 **Deformable Conv** 的 Backbone。
- **预期**: 提升 CER (Character Error Rate) 在困难样本上的表现。

### 💡 Direction C: 预训练范式 (Self-Supervised Learning)

- [cite_start]**痛点**: 论文强调 "No Pre-training" 是为了证明数据高效 [cite: 536, 561]，但为了更高指标，我们可以反其道行之。
- **改进**: 设计针对手写体的 **MAE (Masked Autoencoder)** 预训练任务（不仅仅是随机 Mask，而是基于笔画 Stroke 的 Mask）。
- **预期**: 进一步减少对标注数据的依赖，提升 Few-shot 性能。

## Phase 4: SOTA Sprint & Writing

- [ ] **E4.1**: 确定改进方案，修改模型架构。
- [ ] **E4.2**: 刷榜 (State-of-the-Art)。
- [ ] **E4.3**: 撰写论文。
